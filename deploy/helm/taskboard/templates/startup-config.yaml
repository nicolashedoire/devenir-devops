apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "taskboard.name" . }}-startup
  labels:
    {{- include "taskboard.labels" . | nindent 4 }}
  annotations:
    argocd.argoproj.io/sync-wave: "-2"
data:
  startup-check.mjs: |
    {{- .Files.Get "files/startup-check.mjs" | nindent 4 }}
