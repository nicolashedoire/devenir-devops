global:
  resolve_timeout: 1m
route:
  receiver: journal-local
  group_by: [alertname]
  group_wait: 1s
  group_interval: 5s
  repeat_interval: 1h
receivers:
  - name: journal-local
    webhook_configs:
      - url: http://receiver:8090/alerts
        send_resolved: true
