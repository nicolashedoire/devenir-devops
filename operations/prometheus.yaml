global:
  scrape_interval: 2s
  evaluation_interval: 2s
rule_files: [/etc/prometheus/rules.yaml]
scrape_configs:
  - job_name: taskboard-operations
    static_configs:
      - targets: [api:3000]
alerting:
  alertmanagers:
    - static_configs:
        - targets: [alertmanager:9093]
